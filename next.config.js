/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
    images: {
      domains: ['quaidtech.com'], // Add your domain(s) here
    },
    
  };
// module.exports = nextConfig
